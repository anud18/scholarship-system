# ============================================
# Application Environment Configuration
# ============================================

# Application Environment
# Options: development | test | staging | production
APP_ENV=development
DEBUG=true

# ============================================
# Database Configuration
# ============================================

# AsyncPG format for async operations (primary)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/scholarship_dev

# Sync format for Alembic migrations
DATABASE_URL_SYNC=postgresql://postgres:postgres@localhost:5432/scholarship_dev

# ============================================
# Security & Authentication
# ============================================

# Secret key for JWT (MUST be 32+ characters in production)
SECRET_KEY=your-secret-key-must-be-at-least-32-characters-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# Admin Setup (for seed script)
# ============================================

# First admin user email (used by python -m app.seed)
ADMIN_EMAIL=admin@nycu.edu.tw

# ============================================
# API Settings
# ============================================

API_V1_PREFIX=/api/v1
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================
# NYCU Employee API Configuration
# ============================================

# Client mode: "mock" for development, "http" for staging/production
NYCU_EMP_MODE=mock

# HTTP Client Configuration (for staging/production)
# NYCU_EMP_ACCOUNT=your_api_account
# NYCU_EMP_KEY_HEX=your_hmac_key_in_hex_format
# NYCU_EMP_ENDPOINT=https://your.api.endpoint.com/employee
# NYCU_EMP_INSECURE=false
# NYCU_EMP_TIMEOUT=10.0
# NYCU_EMP_RETRIES=3

# OCR Configuration (Gemini API for bank passbook extraction)
OCR_SERVICE_ENABLED=false
# GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
OCR_TIMEOUT=30
