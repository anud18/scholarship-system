# ============================================
# Production Environment Configuration
# ============================================

# Application Environment
APP_ENV=production
DEBUG=false

# ============================================
# Database Configuration
# ============================================

# AsyncPG format for async operations (primary)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@prod-db:5432/scholarship_production

# Sync format for Alembic migrations
DATABASE_URL_SYNC=postgresql://postgres:postgres@prod-db:5432/scholarship_production

# ============================================
# Security & Authentication
# ============================================

# Secret key for JWT (MUST be 32+ characters and cryptographically secure)
SECRET_KEY=production-secret-key-must-be-changed-to-secure-random-value
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# Admin Setup
# ============================================

# First admin user email (used by python -m app.seed)
ADMIN_EMAIL=admin@nycu.edu.tw

# ============================================
# API Settings
# ============================================

API_V1_PREFIX=/api/v1
CORS_ORIGINS=https://scholarship.nycu.edu.tw

# ============================================
# NYCU Employee API Configuration (PRODUCTION)
# ============================================

# Use HTTP client for production with live credentials
NYCU_EMP_MODE=http

# Production API credentials (replace with actual values)
NYCU_EMP_ACCOUNT=production_account
NYCU_EMP_KEY_HEX=your_production_hmac_key_in_hex_format
NYCU_EMP_ENDPOINT=https://api.nycu.edu.tw/employee
NYCU_EMP_INSECURE=false
NYCU_EMP_TIMEOUT=15.0
NYCU_EMP_RETRIES=3
