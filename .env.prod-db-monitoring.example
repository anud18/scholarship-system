# =========================================================================
# Monitoring Configuration for Production DB-VM
# =========================================================================
# This file contains environment variables for DB-VM monitoring services
#
# Copy this file to .env.prod-db-monitoring and fill in actual values:
#   cp .env.prod-db-monitoring.example .env.prod-db-monitoring
#
# Then edit .env.prod-db-monitoring with your actual AP-VM IP address

# Monitoring Server URL - IP address of AP-VM where Loki/Prometheus/Grafana run
# Format: http://IP_ADDRESS (without port, ports are added in Alloy config)
# Example: http://192.168.1.100 or http://10.0.0.50
MONITORING_SERVER_URL=http://YOUR_AP_VM_IP

# PostgreSQL Database Credentials for Exporter
# CRITICAL: These MUST be set for production deployment
# DO NOT use default values in production
POSTGRES_USER=scholarship_user
POSTGRES_PASSWORD=CHANGE_ME_IN_PRODUCTION
POSTGRES_DB=scholarship_db

# Note: PostgreSQL connection uses sslmode=require for security
# Ensure PostgreSQL is configured to accept SSL connections

# GitHub Secret Integration:
# REQUIRED: Set these variables via GitHub Secrets for production:
#   - Secret name: PROD_MONITORING_SERVER_URL (required)
#   - Secret name: PROD_POSTGRES_USER (required)
#   - Secret name: PROD_POSTGRES_PASSWORD (required)
#   - Secret name: PROD_POSTGRES_DB (optional, defaults to scholarship_db)
#   - Use in workflow:
#       echo "MONITORING_SERVER_URL=${{ secrets.PROD_MONITORING_SERVER_URL }}" > .env.prod-db-monitoring
#       echo "POSTGRES_USER=${{ secrets.PROD_POSTGRES_USER }}" >> .env.prod-db-monitoring
#       echo "POSTGRES_PASSWORD=${{ secrets.PROD_POSTGRES_PASSWORD }}" >> .env.prod-db-monitoring
