# Production Database VM Configuration

# PostgreSQL Configuration
POSTGRES_DB=scholarship_db
POSTGRES_USER=scholarship_user
POSTGRES_PASSWORD=your-very-strong-db-password
POSTGRES_PORT=5432

# MinIO Configuration
MINIO_ROOT_USER=your-minio-access-key
MINIO_ROOT_PASSWORD=your-very-strong-minio-secret-key
MINIO_SERVER_URL=https://your-db-server-domain.edu.tw:9000
MINIO_CONSOLE_URL=https://your-db-server-domain.edu.tw:9001
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Redis Configuration (optional for DB layer caching)
REDIS_PASSWORD=your-very-strong-redis-db-password
REDIS_PORT=6379

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# Security - Network Access
# Only allow connections from specific app server IPs
# Configure in your firewall/security groups:
# - App Server IP: your-app-server-ip
# - Admin access: your-admin-ip-range

# Storage Paths (create these directories on host)
# /opt/scholarship/postgres/data
# /opt/scholarship/postgres/backups
# /opt/scholarship/minio/data
# /opt/scholarship/minio/config
# /opt/scholarship/redis/data