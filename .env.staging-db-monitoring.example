# =========================================================================
# Monitoring Configuration for Staging DB-VM
# =========================================================================
# This file contains environment variables for DB-VM monitoring services
#
# Copy this file to .env.staging-db-monitoring and fill in actual values:
#   cp .env.staging-db-monitoring.example .env.staging-db-monitoring
#
# Then edit .env.staging-db-monitoring with your actual AP-VM IP address

# Monitoring Server URL - IP address of AP-VM where Loki/Prometheus/Grafana run
# Format: http://IP_ADDRESS (without port, ports are added in Alloy config)
# Example: http://192.168.1.100 or http://10.0.0.50
MONITORING_SERVER_URL=http://YOUR_AP_VM_IP

# PostgreSQL Database Credentials for Exporter
# These should match the credentials in docker-compose.staging-db.yml
# Default values are provided but should be changed for security
POSTGRES_USER=scholarship_user
POSTGRES_PASSWORD=scholarship_pass
POSTGRES_DB=scholarship_db

# Note: PostgreSQL connection uses sslmode=require for security
# Ensure PostgreSQL is configured to accept SSL connections

# GitHub Secret Integration:
# You can also set these variables via GitHub Secrets:
#   - Secret name: STAGING_MONITORING_SERVER_URL
#   - Secret name: STAGING_POSTGRES_PASSWORD (recommended for security)
#   - Use in workflow:
#       echo "MONITORING_SERVER_URL=${{ secrets.STAGING_MONITORING_SERVER_URL }}" > .env.staging-db-monitoring
#       echo "POSTGRES_PASSWORD=${{ secrets.STAGING_POSTGRES_PASSWORD }}" >> .env.staging-db-monitoring
